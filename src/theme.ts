import { createTheme } from "@mui/material/styles";

export default createTheme({
  palette: {
    background: {
      default: "#C8EFF9",
    },
    primary: {
      main: "#F27D42",
      dark: "#DF622C",
      light: "#F07C41",
      contrastText: "#FFFFFF",
    },
  },
});
